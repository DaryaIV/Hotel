include ../element-title/element-title.pug

mixin checkbox-list(title)
    //- unless expanded
    .checkbox-list(id= attributes.id)
        +element-title(title)(class= "checkbox-list__title")
        button.checkbox-list__btn
            i.material-icons.checkbox-list__icon expand_more
        .checkbox-list__content
            block
    //- else 
    //-     .checkbox-list.checkbox-list_expanded
    //-         +element-title(title)(class= "checkbox-list__title")
    //-         button.checkbox-list__btn
    //-             i.material-icons.checkbox-list__icon expand_more
    //-         .checkbox-list__content
    //-             block